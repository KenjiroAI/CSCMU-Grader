{% block core %}{% from cms.grading import task_score %}{{ "%20s" % "Username"}} {{ "%30s" % "User"}} {% for task in contest.tasks %}{{ "%14s" % task.name }} {% end %}{{ "%8s" % "Global" }}
{% for user in sorted(main_contest.users, key=lambda u: u.username) %}{% if not user.hidden %}{% set score = 0.0 %}{{ "%20s" % user.username }} {{ "%30s" % ("%s %s" % (user.first_name, user.last_name)) }} {% for task in contest.tasks %}{% set t_score, _ = task_score(user, task) %}{% set t_score = round(t_score, task.score_precision) %}{% set score += t_score %}{{ ("%%13.%dlf" % task.score_precision) % t_score }} {% end %}{{ ("%%7.%dlf" % contest.score_precision) % round(score, contest.score_precision) }}
{% end %}{% end %}{% end %}
